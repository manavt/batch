<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%end%>

<table>
	<!--%form_for is a helper %-->
<%= form_for @user do | form | %>
<tr>
<td><%= form.label :name%></td>
<td><%= form.text_field :name%></td>
</tr>

<tr>
<td><%= form.label :email%></td>
<td><%= form.text_field :email%></td>
</tr>

<tr>
<td><%= form.label :password%></td>
<td><%= form.password_field :password%></td>
</tr>
<tr>
<td><%= form.label :confirm_password%></td>
<td><%= form.password_field :confirm_password%></td>
</tr>

<tr>
<td><%= form.label :phone%></td>
<td><%= form.text_field :phone%></td>
</tr>
<tr>
<td><%= form.label :address%></td>
<td><%= form.text_field :address%></td>
</tr>
<tr>
	<%= form.fields_for :image do | image | %>
     <td><%= image.label :photo%></td>
     <td><%= image.file_field :photo%></td>
   <%end%>
</tr>
<tr>
	<td><%= form.submit "Sign up!"%></td>
	</tr>
<%end%>
</table>

<%= link_to "Back" , root_path%>


